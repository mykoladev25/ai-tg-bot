/**
 * Replicate balance tracking (manual config)
 * Update this when you top up Replicate.
 */

module.exports = {
  // Initial deposit amount in USD
  initialUSD: 100,
  // Date of the initial deposit (YYYY-MM-DD)
  initialDate: '2026-01-26',
  // Additional top-ups
  topUps: [
    // { amountUSD: 50, date: '2026-02-01', note: 'Top-up' }
  ]
};
